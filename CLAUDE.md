# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 1. プロジェクト概要

- 業種: 医療機器メーカー 製造・CAD/CAM領域
- 主要環境: Siemens 840D / Cimatron V15、社内ネットワーク制限あり
- 開発言語: Excel VBA

## 2. コーディング規約（全モジュール共通）

### 2.1 命名規則

- 変数名はすべて日本語で定義する
- 予約語との衝突・曖昧語は禁止
- 接頭辞を明示する：
  - 範囲（Range）→ `rng_`
  - 配列（Array）→ `arr_`
  - 文字列 → `str_`
  - 数値 → `num_` / `int_` / `lng_`
  - ブール値 → `bln_`
  - オブジェクト → `obj_`
- 単位を伴う変数は単位を明記（例：`num_時間_秒`）

### 2.2 コメント規約

- 各行の1行上に `'` で日本語の解説コメントを記載する（省略不可）
- モジュール冒頭に以下の形式でヘッダーを記載：

```vba
'==============================================================
' モジュール名：
' 作成日：
' 作成者：
' 概要：
' 変更履歴：
'==============================================================
```

### 2.3 構造規約

- 処理は機能ごとに `Call` で分割する
- 各Subは `Private Sub` で宣言する（指示がない限り）
- 最初に提示されたSub名は、指示がない限り変更しない
- エラーハンドリングは `On Error GoTo` 方式を標準とする
- 配列は一括読み書き（セル単位ループを避ける）

### 2.4 ログ／トレース規約

- 主要な処理の開始・終了・分岐に `Debug.Print` を挿入する
- フォーマット例：`Debug.Print "[" & Now & "] " & "処理名：開始"`
- 詳細度切替用フラグ変数（例：`bln_詳細ログ出力`）をモジュール冒頭に用意する

### 2.5 文字コード規約

- ファイル入出力時は文字コードを必ず明示する
- `ADODB.Stream` 使用時は `Charset` を明示指定（`"UTF-8"` または `"Shift-JIS"`）
- `Open` ステートメント使用時も想定文字コードをコメントで明記
- 日本語を含むCSV・テキスト出力はBOM付きUTF-8またはShift-JISを前提とする
- 文字コードの前提が不明な場合は、実装前に確認質問をする

### 2.6 テスト・検証規約

モジュール末尾に以下をコメントブロックとして記載：
- テスト項目（想定入力・異常系を含む）
- 検証手順（実行順・期待結果）

### 2.7 納品形式

- 部分修正・差分提示のみで終わらせない
- そのままVBEに貼り付け可能な完全版モジュールを必ず提示する

## 3. 開発ワークフロー

- 計画のあらゆる側面について、共通の認識に達するまで徹底的に質問を投げかける
- 設計ツリーの各枝をたどり、決定事項間の依存関係を一つずつ解決する
- 各質問に対し、推奨する回答も併せて提示する
- 質問は一度に一つずつ行う
- コードベースを探索することで答えが得られる場合は、質問する代わりにコードベースを調査する

## 4. コード解説のスタイル

コードを書くときは、中学生でも理解できるように解説すること。
- 専門用語には必ず簡単な説明を添える
- 何をしているか・なぜそうするかを一言で補足する
- 難しい処理は短く区切って順番に説明する
